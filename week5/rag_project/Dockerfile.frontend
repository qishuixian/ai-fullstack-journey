FROM node:20-alpine AS build
WORKDIR /app
COPY frontend/package*.json ./
RUN npm ci
COPY frontend/ .
COPY frontend/.env.production ./.env.production
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html/ask
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
