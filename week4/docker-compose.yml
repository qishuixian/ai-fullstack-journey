version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      # 使用 bind mount：将宿主机的 ./data 目录挂载到容器的 /app/data
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////app/data/app.db
    restart: unless-stopped 